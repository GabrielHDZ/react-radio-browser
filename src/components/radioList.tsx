import React from 'react'
import { start } from '../helpers/radioList';

function radioList() {

    let result = start();
    console.log(result);

    return (
        <div>data</div>
    )
}

export default radioList